NEXT

* Make compatible with ruby 1.8.7
* Remove rubyzip, nokogiri dependencies
* Remove pry development dependency
* Reimplement using Info-ZIP 'unzip'
* Remove inject_script_tag()
* Remove :contains_zip_file
* Remove destroy_unpacked()
* Hide HTML5ZipFile::File constructor

* Add :size_packed
* Rename :contents_size to :size_unpacked

* Document with YARD
* Replace bin/ with rake tasks
* Remove $LOAD_PATH mangling in lib/ and test/

